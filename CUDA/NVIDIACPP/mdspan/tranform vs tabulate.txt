Using transform:
auto it = thrust::make_counting_iterator(0);

thrust::transform(it, it+5, out.begin(),
                  [] __device__ (int x) {
                      return x * x;
                  });
Using tabulate (simpler):
thrust::tabulate(out.begin(), out.end(),
                 [] __device__ (int i) {
                     return i * i;
                 });

👉 Output:

[0, 1, 4, 9, 16]